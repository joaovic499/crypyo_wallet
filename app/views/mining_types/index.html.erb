<p style="color: green"><%= notice %></p>

<h1>Tipos de Moedas</h1>

<div id="mining_types">
  <% @mining_types.each do |mining_type| %>
    <%= render mining_type %>
    <p>
      <%= link_to t('links.show'), mining_type %>
    </p>
  <% end %>
</div>

<%= link_to t('links.new_coin'), new_mining_type_path %>
<p> <%= link_to "Menu", '/welcome/index' %>
